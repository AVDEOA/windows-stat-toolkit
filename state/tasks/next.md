# Next

- [ ] Add fixture-based tests for parser output and analyzer categorization.
- [ ] Replace or shrink the current remote PowerShell collector with a more native C# diagnostics collection pipeline.
- [ ] Add background runspaces/jobs so long SSH or collection operations do not block the GUI thread.
- [ ] Add richer GUI cards for analyzer findings severity and category trends from saved bundles.
- [ ] Add password-based SSH auth UX or credential vault integration.
- [ ] Promote monitoring from app-open loop to persistent Windows Service/agent mode.
- [ ] Split analyzer generations from the active release path if only `AnalizeV9.ps1` stays user-facing.
