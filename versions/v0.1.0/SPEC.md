# SPEC v0.1.0

## Summary
- Import the legacy Windows diagnostics and analysis toolkit from `C:\CodexTest` into the canonical `NewCodex` package layout.
- Make the toolkit release-ready with docs, wrappers, dossier, and GitHub publication flow.

## Scope
- active script migration into `app/src/`
- runbook, state, and product docs
- PowerShell validation and rebuild wrappers
- initial runtime snapshot and release dossier

## Acceptance
- Product is organized as `app/ops/state/versions`.
- Documentation explains scope, validation flow, and user entrypoints.
- Runnable source bundle can be rebuilt and snapshotted for release.
- Version, changelog, dossier, and GitHub publication are prepared for `v0.1.0`.
