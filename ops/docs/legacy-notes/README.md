# Legacy Notes

This folder preserves the original continuity markdown files that were stored at the root of `C:\CodexTest`.

They are historical context only. Active product planning and memory now live in:

- `state/memory-bank/`
- `state/tasks/`
- `versions/`
